/target

# Scope-tracking working notes are local-only, never tracked.
/scopes/

/resources/

# phora is the tool itself, not a managed dotfiles dir — a root lock is always spurious
/phora.lock
/phora.local.lock

# multi-agent review artifacts
.reviews/
