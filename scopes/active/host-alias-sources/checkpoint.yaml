checkpoint:
  scope_name: host-alias-sources
  scope_path: ./scopes/active/host-alias-sources
  branch: feat/host-alias-sources
  last_batch: 1
  tasks:
    done: [HAS-001]
    pending: [HAS-002, HAS-003, HAS-004, HAS-005, HAS-006]
  next_batch:
    number: 2
    tasks: [HAS-002, HAS-004]   # parallel; both depend on HAS-001
  deferred_issues:
    - "HAS-002: unify forge registry (is_known_host + cli host_templates), add codeberg/sr.ht/bitbucket"
    - "HAS-003: replace // HAS-003 \"\" git bridges with resolved_remote; wire Config::validate into load path"
    - "HAS-005: source_matches NormalizedUrl/MirrorKey identity (// HAS-005 marker at lock.rs)"
  review_config:
    reviewers: [opus, sonnet]
