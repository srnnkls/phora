checkpoint:
  scope_name: worktree-include
  scope_path: ./scopes/draft/worktree-include
  branch: feat/worktree-include
  base: feat/deploy-link-dev-mode
  last_batch: 1
  tasks:
    done: [WTI-001]
    pending: [WTI-002, WTI-006, WTI-003, WTI-004, WTI-005]
  next_batch:
    number: 2
    tasks: [WTI-002]   # primary-worktree detection via gix (WTI-006 depends on it; same new file src/worktree.rs -> sequential)
  deferred_issues:
    - "WTI-003/006: enforce worktree-root containment at apply time (canonicalize + root-prefix); harden Windows-separator/drive/UNC include paths"
  review_config:
    reviewers: [opus, sonnet]
